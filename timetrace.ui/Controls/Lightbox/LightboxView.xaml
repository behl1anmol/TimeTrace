<UserControl x:Class="timetrace.ui.Controls.Lightbox.LightboxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:timetrace.ui.Controls.Lightbox"
             xmlns:vm="clr-namespace:timetrace.ui.Controls.Lightbox"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:LightboxViewModel/>
    </UserControl.DataContext>
    <Grid>

        <!-- Dialog content -->
        <Border Width="400" MinHeight="200" CornerRadius="8" 
        BorderBrush="#CCCCCC" BorderThickness="1"
        HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Prevent clicks on the dialog from triggering the overlay's click event -->
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseDown">
                    <b:InvokeCommandAction Command="{x:Null}"/>
                </b:EventTrigger>
            </b:Interaction.Triggers>

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header with title and close button -->
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="{Binding Title}" 
                       FontSize="20" FontWeight="SemiBold" 
                       HorizontalAlignment="Center"/>
                    <Button Content="✖" Width="24" Height="24"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Command="{Binding CancelCommand}" 
                    Padding="0"
                    Visibility="{Binding ShowCancelButton, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </Grid>

                <!-- Content area -->
                <TextBlock Grid.Row="1" Text="{Binding Message}"
                   TextWrapping="Wrap" HorizontalAlignment="Center" 
                   VerticalAlignment="Center" TextAlignment="Center"/>

                <!-- Button area -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Button Content="{Binding PrimaryButtonText}" Width="130" Height="30" 
                    Margin="0,0,10,0" Command="{Binding PrimaryCommand}"/>
                    <Button Content="{Binding SecondaryButtonText}" Width="130" 
                    Height="30" Margin="10,0,0,0" Command="{Binding SecondaryCommand}"
                    Visibility="{Binding ShowSecondaryButton, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
