<UserControl x:Class="timetrace.ui.Views.NavigationSidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:timetrace.ui.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="220">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:BooleanToStringConverter x:Key="BooleanToStringConverter"/>

        <!-- Nav button style — inherits Fluent theme visuals -->
        <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="14,10"/>
        </Style>
    </UserControl.Resources>

    <Border BorderThickness="0,0,1,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Row 0: Header (App Icon + Title) -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="16,16,16,8">
                <Image Source="/Resources/AppIcon/timeTrace.png"
                       Width="32" Height="32"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                <TextBlock Text="TimeTrace"
                           FontSize="20"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Margin="12,0,0,0"
                           Visibility="{Binding IsCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}"/>
            </StackPanel>

            <!-- Row 1: Navigation Items -->
            <StackPanel Grid.Row="1" Margin="0,8,0,0">

                <!-- Applications Nav Item -->
                <Border BorderThickness="3,0,0,0">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ActiveNavItem}" Value="Applications">
                                    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateToApplicationsCommand}"
                            Tag="Applications">
                        <Button.ToolTip>
                            <ToolTip Content="Running Processes"/>
                        </Button.ToolTip>
                        <StackPanel Orientation="Horizontal">
                            <Viewbox Width="24" Height="24">
                                <Canvas Width="24" Height="24">
                                    <Path Data="M21 2H3c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h7v2H8v2h8v-2h-2v-2h7c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm0 14H3V4h18v12z"
                                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </Canvas>
                            </Viewbox>
                            <TextBlock Text="Applications"
                                       FontSize="14"
                                       FontWeight="Medium"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding IsCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}"/>
                        </StackPanel>
                    </Button>
                </Border>

                <!-- Settings Nav Item -->
                <Border BorderThickness="3,0,0,0">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ActiveNavItem}" Value="Settings">
                                    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateToSettingsCommand}"
                            Tag="Settings">
                        <Button.ToolTip>
                            <ToolTip Content="Settings"/>
                        </Button.ToolTip>
                        <StackPanel Orientation="Horizontal">
                            <Viewbox Width="24" Height="24">
                                <Canvas Width="24" Height="24">
                                    <Path Data="M19.14 12.94c.04-.3.06-.61.06-.94 0-.32-.02-.64-.07-.94l2.03-1.58c.18-.14.23-.41.12-.61l-1.92-3.32c-.12-.22-.37-.29-.59-.22l-2.39.96c-.5-.38-1.03-.7-1.62-.94L14.4 2.81c-.04-.24-.24-.41-.48-.41h-3.84c-.24 0-.43.17-.47.41l-.36 2.54c-.59.24-1.13.57-1.62.94l-2.39-.96c-.22-.08-.47 0-.59.22L2.74 8.87c-.12.21-.08.47.12.61l2.03 1.58c-.05.3-.07.62-.07.94s.02.64.07.94l-2.03 1.58c-.18.14-.23.41-.12.61l1.92 3.32c.12.22.37.29.59.22l2.39-.96c.5.38 1.03.7 1.62.94l.36 2.54c.05.24.24.41.48.41h3.84c.24 0 .44-.17.47-.41l.36-2.54c.59-.24 1.13-.56 1.62-.94l2.39.96c.22.08.47 0 .59-.22l1.92-3.32c.12-.22.07-.47-.12-.61l-2.01-1.58zM12 15.6A3.6 3.6 0 1 1 12 8.4a3.6 3.6 0 0 1 0 7.2z"
                                          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </Canvas>
                            </Viewbox>
                            <TextBlock Text="Settings"
                                       FontSize="14"
                                       FontWeight="Medium"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding IsCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}"/>
                        </StackPanel>
                    </Button>
                </Border>

            </StackPanel>

            <!-- Row 2: Footer (Toggle + Version) -->
            <StackPanel Grid.Row="2" Margin="0,0,0,16">
                <Button Command="{Binding ToggleSidebarCommand}"
                        HorizontalAlignment="Center"
                        Width="32" Height="32"
                        Margin="0,0,0,8">
                    <Button.ToolTip>
                        <ToolTip Content="{Binding IsCollapsed, Converter={StaticResource BooleanToStringConverter}, ConverterParameter='Expand Sidebar|Collapse Sidebar'}"/>
                    </Button.ToolTip>
                    <TextBlock Text="{Binding IsCollapsed, Converter={StaticResource BooleanToStringConverter}, ConverterParameter='▶|◀'}"
                               FontSize="14"/>
                </Button>
                <TextBlock Text="Windows Process Capture v1.0"
                           FontSize="10"
                           HorizontalAlignment="Center"
                           Opacity="0.6"
                           Visibility="{Binding IsCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>