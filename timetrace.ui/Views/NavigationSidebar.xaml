<UserControl x:Class="timetrace.ui.Views.NavigationSidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:timetrace.ui.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <!-- Converters -->
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:BooleanToStringConverter x:Key="BooleanToStringConverter"/>
        
        <!-- Style for sidebar buttons -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="contentPresenter" 
                                            Focusable="False" 
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="#F5F5F5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="#E8E8E8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Toggle button style -->
        <Style x:Key="ToggleButtonStyle" TargetType="Button">
            <!--<Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>-->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="contentPresenter" 
                                            Focusable="False" 
                                            HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="#F0F0F0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border BorderThickness="0,0,1,0" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header with toggle button -->
            <Grid Grid.Row="0" Margin="16,16,12,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>


                <!-- Toggle Button -->
                <Button Grid.Column="0"
                       Command="{Binding ToggleSidebarCommand}">
                    <Button.ToolTip>
                        <ToolTip Content="{Binding IsCollapsed, Converter={StaticResource BooleanToStringConverter}, ConverterParameter='Expand Sidebar|Collapse Sidebar'}"/>
                    </Button.ToolTip>
                  <!--need to fix icons-->
                    <TextBlock Text="{Binding IsCollapsed, Converter={StaticResource BooleanToStringConverter}, ConverterParameter='▶|◀'}" 
                              FontSize="20"/>
                </Button>
              <!-- Logo/Title (only visible when expanded) -->
              <TextBlock Grid.Column="1"
                        Text="TimeTrace"
                        HorizontalAlignment="Center"
                        FontSize="30"
                        FontWeight="SemiBold"
                        VerticalAlignment="Center"
                        Visibility="{Binding IsCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}"/>
            </Grid>

            <!-- Navigation Items -->
            <StackPanel Grid.Row="1" Margin="5,0,16,16">
                <!-- Applications Button -->
                <Button HorizontalContentAlignment="Left"
                        HorizontalAlignment="Stretch"
                        Background="Transparent"
                        Cursor="Hand"
                        BorderThickness="0"
                       Command="{Binding NavigateToApplicationsCommand}">
                    <Button.ToolTip>
                        <ToolTip Content="Applications"/>
                    </Button.ToolTip>
                    <StackPanel Orientation="Horizontal" >
                        <Label Content="📱" FontSize="25" VerticalAlignment="Center"/>
                        <TextBlock Text="Applications" 
                                  FontSize="18"
                                  Margin="12,0,0,0" 
                                  VerticalAlignment="Center"
                                  FontWeight="Medium"
                                  Visibility="{Binding IsCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}"/>
                    </StackPanel>
                </Button>

                <!-- Settings Button -->
                <Button HorizontalContentAlignment="Left"
                        HorizontalAlignment="Stretch"
                        Background="Transparent"
                        Cursor="Hand"
                        BorderThickness="0"
                        Command="{Binding NavigateToSettingsCommand}">
                    <Button.ToolTip>
                        <ToolTip Content="Settings"/>
                    </Button.ToolTip>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="⚙️" FontSize="25" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        <TextBlock Text="Settings" 
                                  FontSize="18"
                                  Margin="12,0,0,0" 
                                  VerticalAlignment="Center"
                                  FontWeight="Medium"
                                  Visibility="{Binding IsCollapsed, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=True}"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>