<UserControl x:Class="timetrace.ui.Views.ApplicationDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:timetrace.ui.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- FilterView (Left Side) -->
        <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
            <local:FilterView DataContext="{Binding FilterViewModel}"/>
        </Border>

        <!-- PresenterView (Right Side) -->
        <Border Grid.Column="1" Background="White">
            <local:PresenterView DataContext="{Binding PresenterViewModel}"/>
        </Border>
    </Grid>
</UserControl>
