<UserControl x:Class="timetrace.ui.Views.ApplicationListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:timetrace.ui.Helpers"
             xmlns:converters="clr-namespace:timetrace.ui.Converters"
             xmlns:models="clr-namespace:timetrace.ui.Models"
             Background="Transparent"
             helpers:ViewLocator.ViewModel="ApplicationListViewModel">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Currently Running Applications"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
            <TextBlock Text="Click on any application to view captured screenshots"
                       FontSize="13"
                       Margin="0,4,0,0"
                       Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
        </StackPanel>

        <!-- Card Grid -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Applications}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:ApplicationModel}">
                        <Border x:Name="CardBorder"
                                Width="300"
                                Margin="0,0,16,16"
                                Padding="16"
                                CornerRadius="12"
                                BorderThickness="1"
                                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                                Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                                Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                              Command="{Binding DataContext.SelectApplicationCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                              CommandParameter="{Binding}"/>
                            </Border.InputBindings>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Emoji Icon -->
                                <TextBlock Grid.Column="0"
                                           Text="{Binding IconPath}"
                                           FontSize="28"
                                           VerticalAlignment="Center"
                                           Margin="0,0,14,0"/>

                                <!-- App Info -->
                                <StackPanel Grid.Column="1"
                                            VerticalAlignment="Center">
                                    <TextBlock Text="{Binding ProcessName}"
                                               FontSize="16"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <StackPanel Orientation="Horizontal"
                                                Margin="0,4,0,0">
                                        <TextBlock Text="📷"
                                                   FontSize="12"
                                                   Margin="0,0,4,0"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding CaptureCount, StringFormat='{}{0} screenshots captured'}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Chevron -->
                                <TextBlock Grid.Column="2"
                                           Text="→"
                                           FontSize="18"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                                           Margin="8,0,0,0"/>
                            </Grid>
                        </Border>

                        <DataTemplate.Triggers>
                            <Trigger SourceName="CardBorder" Property="IsMouseOver" Value="True">
                                <Setter TargetName="CardBorder" Property="Background"
                                        Value="{DynamicResource SystemControlHighlightListLowBrush}"/>
                                <Setter TargetName="CardBorder" Property="BorderBrush"
                                        Value="{DynamicResource SystemControlHighlightListMediumBrush}"/>
                            </Trigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
