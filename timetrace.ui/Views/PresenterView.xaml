<UserControl x:Class="timetrace.ui.Views.PresenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:timetrace.ui.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Gallery Header -->
        <StackPanel Grid.Row="0" Margin="20,16,20,8">
            <TextBlock Text="Screenshot Gallery" FontSize="18" FontWeight="SemiBold"/>
            <TextBlock Text="Captured images with timestamps" FontSize="12"
                       Opacity="0.6" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Image Gallery -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="12,0">
            <ItemsControl AlternationCount="10000"
                          ItemsSource="{Binding FilteredImages}"
                          Margin="8">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.GoToImageCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding Path=(ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}}"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="CardBorder"
                                            BorderThickness="1"
                                            CornerRadius="8" Margin="6" Padding="10">
                                        <StackPanel>
                                            <!-- Image Placeholder -->
                                            <Grid Height="140">
                                                <Border CornerRadius="4" Opacity="0.08"
                                                        Background="Gray"/>
                                                <TextBlock Text="📷" FontSize="32"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           Opacity="0.5"/>
                                                <Image Source="{Binding ImageSource}"
                                                       Stretch="UniformToFill"
                                                       RenderOptions.BitmapScalingMode="HighQuality"
                                                       VerticalAlignment="Stretch"/>
                                            </Grid>

                                            <!-- Image Details -->
                                            <TextBlock Text="{Binding ScreenshotName}"
                                                       FontWeight="SemiBold" FontSize="13"
                                                       TextTrimming="CharacterEllipsis"
                                                       Margin="0,8,0,0"/>
                                            <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:MMM dd, yyyy HH:mm}'}"
                                                       FontSize="11" Opacity="0.6"
                                                       Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="CardBorder" Property="Opacity" Value="0.8"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
