<UserControl x:Class="timetrace.ui.Views.PresenterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:timetrace.ui.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Gallery Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="16">
            <TextBlock Text="Captured Images" FontSize="16" FontWeight="SemiBold" 
                       VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ImageCount, StringFormat='({0} images)'}" 
                       FontStyle="Italic" Margin="8,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Image Gallery -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding CapturedImages}" Margin="16">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" BorderBrush="#E0E0E0" 
                                BorderThickness="1" CornerRadius="4" 
                                Margin="4" Padding="8">
                            <StackPanel>
                                <!-- Image Placeholder -->
                                <Border Height="120" Background="#F5F5F5" 
                                        CornerRadius="2" Margin="0,0,0,8">
                                    <TextBlock Text="📷" FontSize="32" 
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center" 
                                               Opacity="0.6"/>
                                </Border>

                                <!-- Image Details -->
                                <TextBlock Text="{Binding ScreenshotName}" 
                                           FontWeight="SemiBold" FontSize="12" 
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:MM/dd/yyyy HH:mm}'}" 
                                           FontSize="10" Foreground="#666" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding FileSize}" 
                                           FontSize="10" Foreground="#666"/>
                                <TextBlock Text="{Binding ProcessStatus}" 
                                           FontSize="10" Foreground="#0066CC" 
                                           FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
